/** @type {import('next').NextConfig } */
const nextConfig = {
  // Remove deprecated experimental.appDir as it's default in Next.js 15
  // Remove TypeScript compiler options as they belong in tsconfig.json
};

export default nextConfig;